# Ignore common unnecessary files to keep image build context small
.git
__pycache__
*.pyc
.venv
.env
.pytest_cache
/.vscode
*.log

# Keep the `data/` directory by default so examples work; if you want to mount
# host data at runtime, remove it from the image and bind-mount the folder.